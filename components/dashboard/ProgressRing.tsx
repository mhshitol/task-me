// TODO Phase 1: SVG circular progress indicator
// Props: completed, total
export function ProgressRing({ completed, total }: { completed: number; total: number }) {
  return <div className="w-20 h-20 rounded-full bg-muted" />;
}
