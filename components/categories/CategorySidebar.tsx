// TODO Phase 1: sidebar category list with task counts
import type { Category } from "@/types";
export function CategorySidebar({ categories }: { categories: Category[] }) {
  return <nav className="flex flex-col gap-1 px-3" />;
}
